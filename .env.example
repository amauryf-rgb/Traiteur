DATABASE_URL="postgresql://postgres:postgres@localhost:5432/traiteur_dev"
# Secret utilisé pour signer les sessions employé/propriétaire (interface pro).
# Génère le tien : node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
AUTH_SECRET="change-me"
